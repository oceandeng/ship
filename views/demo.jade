extends layout

block content
	h1= title
	p #{title}
	form(action="/demo", method="post")
		input(type="text")
		input(type="submit", value="aaa")
	section(class="container")
		nav(id="menu", class="draggable") 菜单
	div(id="myDiv") 1
	div(id="output") 2
